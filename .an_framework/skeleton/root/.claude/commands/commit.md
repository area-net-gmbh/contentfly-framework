---
description: Commit the current changes following the git conventions
argument-hint: "[EEE-SSS-TTTT] [url] [--de|--en]"
---
Read `.an_framework/commands/commit.md` and follow it exactly.

Arguments: $ARGUMENTS

If `.an_framework/commands/commit.md` does not exist, **stop immediately**. Report:
"Framework not initialised — `.an_framework/` is missing. Re-run the framework setup, or restart Claude from the project root."
Stage nothing, commit nothing, and do not improvise a commit format.
