---
description: Reflect on a filled-out artifact, then polish it
argument-hint: "<file>"
---
Read `.an_framework/commands/refine.md` and follow it exactly.

Arguments: $ARGUMENTS

If `.an_framework/commands/refine.md` does not exist, **stop immediately**. Report:
"Framework not initialised — `.an_framework/` is missing. Re-run the framework setup, or restart Claude from the project root."
Rewrite nothing and do not improvise the workflow.
