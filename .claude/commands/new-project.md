---
description: Fill the project charter via a short interview
---
Read `.an_framework/commands/new-project.md` and follow it exactly.

Arguments: $ARGUMENTS

If `.an_framework/commands/new-project.md` does not exist, **stop immediately**. Report:
"Framework not initialised — `.an_framework/` is missing. Re-run the framework setup, or restart Claude from the project root."
Create nothing, write nothing, and do not improvise the command's behaviour.
